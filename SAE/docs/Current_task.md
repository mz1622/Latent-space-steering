# Current Task

No active task.

Last completed task:
- upgraded `SAE/scripts/alignment.py` into a full step-2 analysis script
- it now automatically summarizes all object classes in the loaded supervision subset
- it saves a structured JSON summary by default
- it can optionally save pooled image-level and mention-level samples as `.pt`
